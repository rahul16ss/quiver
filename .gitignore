# ─── Dependencies ─────────────────────────────────────────────────────
node_modules/

# ─── Build / release artifacts ────────────────────────────────────────
dist/
dist-electron/
build/
out/
release/
*.tsbuildinfo
# Packaged Electron binaries
*.app
*.dmg
*.exe
*.AppImage
*.snap
*.deb
*.rpm

# ─── Secrets & local env ──────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
.env.local

# ─── Local runtime state (never commit user data) ─────────────────────
.sessions/
.quiver/
.agents/
input.txt
*.session
*.state.json

# ─── Logs & coverage ──────────────────────────────────────────────────
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage/
.nyc_output/
*.lcov
.tap/

# ─── OS junk ──────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ─── Editor / IDE ─────────────────────────────────────────────────────
.vscode/
.idea/
.cursor/
.history/
*.swp
*.swo
*~

# ─── Misc scratch ─────────────────────────────────────────────────────
*.tmp
*.bak
*.orig
.quiver_cloud_notice_shown*.rtf
gitignore and remember.rtf

# ─── Private vendor blueprint (not part of the open-source release) ───
.spec-swimlane.md
docs/ICP.md

# ─── GitHub Actions (requires workflow scope on PAT) ───────────────────
# Remove this line once your GitHub PAT has the 'workflow' scope added,
# then: git add .github/ && git commit && git push
.github/workflows/release.yml

# ─── Per-developer agent config (same class as .vscode/.idea/.cursor) ─
.claude/
.codex/

# ─── Runtime backup directories (atomic-write helper writes *.bak beside ─
# ─── every edited file; keep them out of the repo) ────────────────────
.quiver-backups/

# ─── Test / stress output ─────────────────────────────────────────────
stress-test-results/
review-tests/
tests/owner_verification/

# ─── Internal QA audit scratch (not part of the OSS release) ──────────
.spec-audit-findings.md
