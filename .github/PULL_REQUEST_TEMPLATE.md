## Summary

Brief description of what this PR changes and why.

## Verification

- [ ] `npm test` green
- [ ] `npx tsc --noEmit` clean
- [ ] `npm run demo:ic-memo` 8/8
- [ ] If GUI changed: visual walkthrough done (launch → send → approval → deliverable → settings)

## Public claims

If this PR changes a capability, does the capability truth table in the Conviction Studio repo need updating? (If unsure, leave unchecked and the maintainer will review.)